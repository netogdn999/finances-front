name: navigator
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1

environment:
  sdk: ">=2.14.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^7.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  facily_lints:
    path: ../facily_lints  
  bloc_test: ^8.5.0
  mocktail: ^0.2.0

flutter:
